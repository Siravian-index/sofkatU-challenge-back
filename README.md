# sofkatU-challenge-back

- git clone
- cd sofkatU-challenge-back
- install dependencies
- update application.properties file with your db information
- (keep the server.port as 8080)
- spin up the server
